# 排除 node_modules（容器内会重新执行 npm ci 安装）
# 这是最重要的一条！否则会把本地几百MB的依赖全部复制进构建上下文，极慢
node_modules/

# 排除上一次的构建产物（容器内重新构建）
dist/

# 排除 IDE 和系统文件
.idea/
.vscode/
.DS_Store
*.log

# 排除 git 相关文件
.git/
.gitignore

# 排除环境变量文件（前端构建时不需要）
.env
.env.*

# 排除 README 和文档
README.md
