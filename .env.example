# =============================================================
# 环境变量配置模板
# 使用方式：cp .env.example .env，然后填写真实值
# !! 警告：.env 文件包含敏感信息，绝对不能提交到 Git !!
#
# 部署模式说明：
#   本地 docker-compose : 使用下方 DATABASE_NAME / DATABASE_USERNAME /
#                         DATABASE_PASSWORD，compose 自动组装 DATABASE_URL
#   云托管单容器         : 直接传入 DATABASE_URL（完整 JDBC 连接串）以及
#                         REDIS_HOST / REDIS_PORT 指向云托管 Redis 实例
# =============================================================

# ── 数据库配置 ─────────────────────────────────────────────────
# 【docker-compose 模式】以下三项由 compose 自动组装 DATABASE_URL
DATABASE_NAME=doc_translation
DATABASE_USERNAME=your_db_user
DATABASE_PASSWORD=your_strong_db_password

# 【云托管单容器模式】直接提供完整连接串，优先级高于上面三项
# DATABASE_URL=jdbc:postgresql://<cloud-pg-host>:5432/doc_translation

# ── Redis 配置（云托管模式必填）────────────────────────────────
# docker-compose 模式下由 compose 注入，无需填写
# 云托管模式下请填写云 Redis 实例地址
REDIS_HOST=localhost
REDIS_PORT=6379

# ── GitHub App 配置 ────────────────────────────────────────────
# 在 https://github.com/settings/apps 创建 App 后获取
GITHUB_APP_ID=123456
GITHUB_APP_CLIENT_ID=Iv1.xxxxxxxxxxxxxxxx
GITHUB_APP_CLIENT_SECRET=xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
GITHUB_APP_WEBHOOK_SECRET=your_webhook_secret
# GitHub App 的 slug（URL 中显示的短名称）
GITHUB_APP_SLUG=doc-translation-github-app
# GitHub App 私钥：将 PEM 文件内容中的换行符替换为 \n 字面量 ，使用命令：cat path/to/private-key.pem | tr '\n' '\n'
# 示例格式：GITHUB_APP_PRIVATE_KEY=-----BEGIN RSA PRIVATE KEY-----\nMIIEogIBAAJK...
GITHUB_APP_PRIVATE_KEY=-----BEGIN RSA PRIVATE KEY-----\nYOUR_PRIVATE_KEY_HERE

# ── AI 翻译配置 ────────────────────────────────────────────────
# 在 https://openrouter.ai/keys 获取
OPENROUTER_API_KEY=sk-or-v1-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# ── 应用安全配置 ───────────────────────────────────────────────
# JWT_SECRET：用于签发/验证 JWT，至少 32 位随机字符串
# 生成命令：openssl rand -hex 32
JWT_SECRET=change-me-to-a-random-64-char-string-please-do-not-use-this

# ENCRYPTION_KEY：用于加密存储在数据库中的 API Key，必须正好 32 字符
# 生成命令：openssl rand -hex 16
ENCRYPTION_KEY=change-me-32-char

# ── 前端访问地址 ───────────────────────────────────────────────
# GitHub OAuth 登录成功后重定向的地址
# 本地：http://localhost
# 生产：https://yourdomain.com
FRONTEND_URL=http://localhost
